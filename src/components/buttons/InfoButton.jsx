export default function InfoButton({ styles }) {
  return (
    <button
      className={`${styles} p-5 w-4 h-4 bg-white rounded-full text-gray-900 font-bold flex justify-center items-center`}
    >
      i
    </button>
  );
}
